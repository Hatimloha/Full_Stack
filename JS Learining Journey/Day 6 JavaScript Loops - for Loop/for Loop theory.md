# Theory:
> A loop lets you run the same block of code multiple times — perfect for tasks like repeating messages, iterating over arrays, etc.

### Syntax
```js
for (initialization; condition; increment) {
  // code to execute each loop
}
```

