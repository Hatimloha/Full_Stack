// Create a login system:
// If username is "hatim" and password is "1234", print "Login successful"
// Else print "Invalid credentials"

let username = "hatim";
let pass = 1234;
if(username === "hatim" && pass === 1234){
    console.log("Login successful")
}
else{
    console.log("Invalid credentials")
}

